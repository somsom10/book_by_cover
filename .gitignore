# Raw data: ~10 GB, fetched by download/fetch_data.py
comention/goodreads/
comention/goodreads_books.json
comention/goodreads_books.json.gz

# Large regenerable intermediates (rebuild with `python3 main.py`)
comention/author_genres.csv
comention/blurb_mentions.csv
comention/blurb_mentions_clean.csv

__pycache__/
*.pyc
.claude/

# year_genre_prediction: its dataset and its trained models / figures
year_genre_prediction/data/
year_genre_prediction/outputs/

# themes: its raw data (the 2 GB archive lives here and comention/ expands
# from it) and everything its pipeline generates
themes/data/
themes/work/
